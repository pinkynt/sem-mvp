import { dashboardApiError, requireDashboardUser } from "@/server/dashboard/auth";
import { setPermitHolderActive, upsertPermitHolder } from "@/server/dashboard/commands";
export const runtime = "nodejs"; export const dynamic = "force-dynamic";
export async function PATCH(request: Request, { params }: { params: Promise<{ id: string }> }) { try { const user = await requireDashboardUser(); const { id } = await params; const body = await request.json(); if (typeof body.active === "boolean" && Object.keys(body).length === 1) { await setPermitHolderActive(id, body.active); return Response.json({ ok: true }); } return Response.json(await upsertPermitHolder({ ...body, id }, user.id)); } catch (error) { return dashboardApiError(error, "No se pudieron guardar los cambios. Revisá los datos e intentá nuevamente."); } }
